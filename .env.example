# PartyKit environment variables

# For local development, PartyKit runs on localhost:1999 by default
# For production, this will be your deployed PartyKit URL
# Example: NEXT_PUBLIC_PARTYKIT_HOST=your-project-name.your-username.partykit.dev

# Local development (automatically used when running `npx partykit dev`)
# NEXT_PUBLIC_PARTYKIT_HOST=localhost:1999

# Production (set this in Vercel environment variables)
# NEXT_PUBLIC_PARTYKIT_HOST=windows-xd-chat.YOUR_USERNAME.partykit.dev
